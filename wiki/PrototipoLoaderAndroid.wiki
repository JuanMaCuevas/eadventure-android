#summary Anotaciones Implementacion Loader SAX Android
#sidebar BarraNavegacion

= Introduccion =

Aquí voy a anotar los cambios y decisiones que voy tomando para realizar la integración del loader en Android . 

= Apuntes =

 * Añado clase Polygon sacada de URL : .......  y añado metodo addPoint ...
 * Cambio clases Point por las de Android , falta metodo Clone -> hago cambio me creo un nuevo objeto y lo construyo con copia del point a clonar .
 * Me cargo el parser y toda la informacion necesaria para la carga de animaciones
 * Quito cosas que no se usan , dejo en el paquete es.eucm.eadventure.common.auxiliar solo la clase assetsConstants
 * Al no tener JOptionPane para mostrar mensajes ( se usa en varias partes ) he creado una clase que en el fondo reimplementa los metodos con Alerts de Android.
 * Problemilla :  The default Android implementation of SAXParser supports only the following three features:

http://xml.org/sax/features/namespaces
    Queries the state of namespace-awareness.
http://xml.org/sax/features/namespace-prefixes
    Queries the state of namespace prefix processing
http://xml.org/sax/features/validation
    Queries the state of validation.

Note that despite the ability to query the validation feature, there is currently no validating parser available. Also note that currently either namespaces or namespace prefixes can be enabled, but not both at the same time.

 ISSUE 990: http://code.google.com/p/android/issues/detail?id=990 No referencia bien sin namespaces los qname 


 -> para solucionar cambio los qName por sName de los Handler ( ChapterHandler y DescriptorHandler)
 -> cambio todos los getQName por getLocalName de los Handler ( ChapterHandler y DescriptorHandler)

Basicamente lo de arriba lo tengo que hacer para todos los Handler .. se van a acabar usando casi todos... asi que lo cambio en todos los parsers del paquete Loader.parser y loader.subparsers

 * Pasar datos a SD -> bug en DDMS utilizar comando consola :
  * Meter datos : adb push foo.txt /sdcard/foo.txt
  * Sacar datos : adb pull /sdcard/foo.txt foo.txt 

 * Metodos del editor que no usa el motor ( Proxima iteracion del prototipo , limpio las clases ) :
  * Clase Loader : loadAdaptationProfile(InputStreamCreator, String, List<Incidence>) loadAdventureData(InputStreamCreator, List<Incidence>, boolean)


 * Null pointer exception en : AssesmentSubParser debido a una declaracion doble de currentString , la que se declara localmente y la que se declara en la clase padre SubParser...